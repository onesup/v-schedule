<p id="notice"><%= notice %></p>

<p>
  <b>생일 :</b>
  <%=l @baby.born_at.to_date, :format => :default %>
</p>

<table>
  <tr>
    <th>Title</th>
    <th>Desc</th>
    <th>Timing</th>
    <th>Contury</th>
    <th>Series</th>
    <th>Nip</th>
    <th>Injection_type</th>
  </tr>

<% @vaccinations.each do |vaccination| %>
  <tr>
    <td><%= vaccination.title %></td>
    <td><%= vaccination.desc %></td>
    <% unless vaccination.least.nil?  %>
    <td>접종기간은 <%=l (@baby.born_at + vaccination.timing).to_date, :format => :default  %> ~ <%=l (@baby.born_at + vaccination.least).to_date, :format => :default  %> 입니다. (<%= time_ago_in_words(@baby.born_at + vaccination.timing) %> 남았습니다.)</td> 
    <% else %>
    <td>접종일은 <%=l (@baby.born_at + vaccination.timing).to_date, :format => :default  %> 입니다. (<%= time_ago_in_words(@baby.born_at + vaccination.timing) %> 남았습니다.)</td>
    <% end %>
    <td><%= vaccination.contury %></td>
    <td><%= vaccination.series %></td>
    <td><%= vaccination.nip %></td>
    <td><%= vaccination.injection_type %></td>
  </tr>
<% end %>
</table>

<%= link_to 'Back', new_main_baby_path %>
